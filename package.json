{
  "name": "reducer-tester",
  "version": "1.0.1",
  "description": "reducer tester",
  "license": "MIT",
  "repository": "akameco/reducer-tester",
  "author": "akameco <akameco.t@gmail.com> (akameco.github.io)",
  "engines": {
    "node": ">=6"
  },
  "scripts": {
    "add-contributor": "all-contributors add",
    "fmt": "prettier --write '**/*.{js,json,md}'",
    "build": "babel src -d dest --ignore *.test.js",
    "prepare": "yarn build",
    "flow": "flow",
    "lint": "eslint .",
    "test": "jest",
    "test:cov": "jest --coverage --ci --runInBand",
    "test:ci": "yarn flow && yarn lint && yarn test:cov"
  },
  "lint-staged": {
    "*.{js,json,md}": [
      "prettier --write",
      "git add"
    ]
  },
  "files": [
    "dest",
    "types/index.d.ts"
  ],
  "main": "dest",
  "types": "types/index.d.ts",
  "keywords": [
    "test",
    "jest",
    "redux",
    "react",
    "snapshot",
    "diff",
    "reducer",
    "test"
  ],
  "dependencies": {
    "invariant": "^2.2.4",
    "snapshot-diff": "^0.4.0"
  },
  "devDependencies": {
    "@babel/cli": "7.1.2",
    "@babel/core": "7.1.2",
    "all-contributors-cli": "5.4.1",
    "babel-core": "7.0.0-bridge.0",
    "babel-jest": "23.6.0",
    "babel-preset-zero": "0.6.2",
    "eslint": "5.8.0",
    "eslint-config-precure": "3.3.0",
    "flow-bin": "0.85.0",
    "husky": "1.1.3",
    "jest": "23.6.0",
    "lint-staged": "8.0.4",
    "prettier": "1.14.3"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn flow && lint-staged"
    }
  },
  "jest": {
     "modulePathIgnorePatterns": ["dest"]
  }
}
